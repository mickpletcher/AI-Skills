# AI Skills Changelog

## 2026-05-14

1. Added explicit `Help And Examples` sections to every Claude `SKILL.md` so each skill can explain what it does, state the minimum useful input, and show example prompts when the user is not sure how to use it.

2. Updated the shared skill authoring sources to carry the same help behavior forward in new work:
   `shared/skill-standard.md`
   `templates/skill-template.md`
   `templates/claude-skill-template.md`
   `claude/SKILL-template.md`

3. Rebuilt the packaged Claude skill exports so the `.skill` files match the updated source files.

4. Expanded the root local planning backlog in `future-projects.md` with a much broader set of ChatGPT, Claude, GitHub, shared-platform, and repo-wide ideas.

5. Updated the root `README.md` so the Claude skill catalog matches the actual skill folders, including the missing entries for:
   `fitness-programming`
   `github-social-preview`
   `patentforge-provisional-skill`

6. Added the root `completed-projects.md` file and moved completed additions out of `future-projects.md` so future work and completed work are tracked separately.

7. Updated the root `README.md` to link to `completed-projects.md` and restored the root changelog link for repo discoverability.
